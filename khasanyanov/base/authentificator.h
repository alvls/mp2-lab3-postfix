#ifndef __AUTHENTIFICATOR_H__
#define __AUTHENTIFICATOR_H__

#include <string>
#include"stack.h"

inline bool isOperator(const char c);
void autentificateInfix(const string& infix);
string eraseSpaces(const string& s);

#endif
