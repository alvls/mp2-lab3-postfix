language: cpp
sudo: false
compiler:
  - gcc
  - clang
before_script:
  - mkdir ../build
  - cd ../build
script:
  - cmake ../mp2-lab3-postfix/Сысоев
  - make -j
  - ls -la
  - cd /base/
  - ls -la
  - cd ../../
  - ls -la
  - /build/postfix
notifications:
  email: false
