language: cpp
sudo: false
compiler:
  - gcc
  - clang
before_script:
  - mkdir ../build
  - cd ../build
script:
  - cmake ../mp2-lab3-postfix/Сысоев
  - make -j
  - cd ./bin/
  - ./postfix
notifications:
  email: false
